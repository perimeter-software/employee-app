'use client';

// Placeholder port of stadium-people MessageSection (42 lines).
const MessageSection: React.FC<{ isAvailable: boolean }> = ({ isAvailable }) => {
  if (!isAvailable) return null;
  return null;
};

export default MessageSection;
